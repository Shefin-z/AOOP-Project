import StudentWorkspace from "../../components/student/StudentWorkspace";

export const metadata = { title: "Student workspace — CareerForge" };

export default function StudentPage() {
  return <StudentWorkspace />;
}
