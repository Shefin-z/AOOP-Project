import AuthExperience from "../../../components/AuthExperience";

export const metadata = { title: "Student sign in — CareerForge" };

export default function StudentLoginPage() {
  return <AuthExperience role="student" />;
}
