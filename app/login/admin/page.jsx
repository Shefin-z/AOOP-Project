import AuthExperience from "../../../components/AuthExperience";

export const metadata = { title: "Admin access — CareerForge" };

export default function AdminLoginPage() {
  return <AuthExperience role="admin" />;
}
